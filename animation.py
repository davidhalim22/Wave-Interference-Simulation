import numpy as np
import matplotlib.pyplot as plt
from matplotlib.animation import FuncAnimation

from config import (GRID_SIZE, X_MIN, X_MAX, Y_MIN, Y_MAX)

def animate_wave():
    plt.title("Wave Interference Animation")
    plt.show()